/**
 * NOTE: Add colors present in the SVG files here.
 * Ideally, SVGs should not have to mention any color.
 * All SVGs should only use one specific color for fill, another for stroke, etc if needed.
 */

// module.exports = {
//   replaceAttrValues: {
//     '#121212': '{props.fill}',
//     '#fefefe': '{props.stroke}',
//   },
// }
